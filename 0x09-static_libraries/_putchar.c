#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _putchar - Prints a character to stdout
 * @c: The character to be printed
 *
 * Return: On success, returns the character printed (c).
 * On error, returns EOF.
 */
int _putchar(char c)
{
	return (putchar(c));
}
